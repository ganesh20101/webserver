---
- hosts : webserver
  become : True
  tasks :
   - name : apache
     apt : 
      name: apache2
      state: present
   - name : servive start
     service:
      name: apache2
      state: started
   - name : copy file
     copy:
      src: index.html
      dest: /var/www/html/
